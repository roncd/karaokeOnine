---
# Publication des images Docker sur GHCR
name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: Version to publish, for example v1.0.0
        required: true
        type: string

permissions:
  contents: write
  packages: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

defaults:
  run:
    shell: bash

env:
  BACKEND_IMAGE: ghcr.io/${{ github.repository_owner }}/karaoke-backend
  FRONTEND_IMAGE: ghcr.io/${{ github.repository_owner }}/karaoke-frontend

jobs:
  validate-version:
    name: Validate version
    runs-on: ubuntu-latest
    timeout-minutes: 5

    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Resolve version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            version="${{ inputs.version }}"
          else
            version="${GITHUB_REF_NAME}"
          fi

          if [[ ! "$version" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid version: $version"
            echo "Expected format: vMAJOR.MINOR.PATCH"
            exit 1
          fi

          echo "version=$version" >> "$GITHUB_OUTPUT"

  build-and-publish-backend:
    name: Build and publish image backend
    runs-on: ubuntu-latest
    needs:
      - validate-version
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image backend
        uses: docker/build-push-action@v6
        with:
          context: backend
          file: backend/Dockerfile
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:${{ needs.validate-version.outputs.version }}
            ${{ env.BACKEND_IMAGE }}:latest
          labels: |
            org.opencontainers.image.title=Karaoke O'nine backend
            org.opencontainers.image.version=${{ needs.validate-version.outputs.version }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
          build-args: |
            BUILD_VERSION=${{ needs.validate-version.outputs.version }}
            VCS_REF=${{ github.sha }}
            BUILD_DATE=${{ github.event.repository.updated_at }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-and-publish-frontend:
    name: Build and publish image frontend
    runs-on: ubuntu-latest
    needs:
      - validate-version
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image frontend
        uses: docker/build-push-action@v6
        with:
          context: frontend
          file: frontend/Dockerfile
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE }}:${{ needs.validate-version.outputs.version }}
            ${{ env.FRONTEND_IMAGE }}:latest
          labels: |
            org.opencontainers.image.title=Karaoke O'nine frontend
            org.opencontainers.image.version=${{ needs.validate-version.outputs.version }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
          build-args: |
            BUILD_VERSION=${{ needs.validate-version.outputs.version }}
            VCS_REF=${{ github.sha }}
            BUILD_DATE=${{ github.event.repository.updated_at }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  github-release:
    name: Create GitHub release
    runs-on: ubuntu-latest
    needs:
      - validate-version
      - build-and-publish-backend
      - build-and-publish-frontend
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Create release notes
        run: |
          cat > release-notes.md <<'MARKDOWN'
          ## Summary

          - Published Docker image to GitHub Container Registry.
          - Created release from a semantic version tag.

          ## Image

          ### Backend
          ```text
           ${{ env.BACKEND_IMAGE }}:${{ needs.validate-version.outputs.version }}
          ```
          ### Frontend
          ```text
          ${{ env.FRONTEND_IMAGE }}:${{ needs.validate-version.outputs.version }}
          ```

          ## Staging deploy
          ```bash
          docker compose --env-file ops/overlays/staging/.env up -d
          ```
          MARKDOWN

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.validate-version.outputs.version }}
          name: Release ${{ needs.validate-version.outputs.version }}
          body_path: release-notes.md
